apiVersion: v1
kind: Service
metadata:
  name: {{ include "llama-stack-server.fullname" . }}
  labels:
    {{- include "llama-stack-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llama-stack-server.selectorLabels" . | nindent 4 }} 