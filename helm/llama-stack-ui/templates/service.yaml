apiVersion: v1
kind: Service
metadata:
  name: {{ include "llama-stack-ui.fullname" . }}
  labels:
    {{- include "llama-stack-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llama-stack-ui.selectorLabels" . | nindent 4 }} 