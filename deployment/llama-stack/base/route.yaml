---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: llamastack-with-config
  labels:
    app: llamastack-with-config
spec:
  to:
    kind: Service
    name: llamastack-with-config-service
    weight: 100
  port:
    targetPort: 8321
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None

